apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerBinding
metadata:
  name: parasol-insurance-secured-tag-binding
  namespace: {{ .Values.namespace }}
spec:
  params:
    - name: git-tag
      value: $(body.ref)
    - name: git-revision
      value: $(body.checkout_sha)
